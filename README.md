# mem-mesh

Universal AI memory layer — one persistent memory for all your AI tools.

mem-mesh runs as a local proxy daemon that intercepts AI API calls and injects
persistent memory across Claude, Cursor, and any other tool that speaks the
Anthropic or OpenAI API protocol.

## Install

```bash
pip install mem-mesh
```

## Usage

```bash
mem-mesh --help
```
